#include "holberton.h"
/**
 *
 *
 *
 */
void print_rev(char *s)
{
  int temp, start, end;
  start = 0;
end =
  while (start < end)
    {
      temp = arr[start];   
      arr[start] = arr[end];
      arr[end] = temp;
      start++;
      end--;
    }   
}     
 
