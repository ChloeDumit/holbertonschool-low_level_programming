#include "holberton.h"
#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _abs(int x)
{
  if (x < 0) {
    return ((-x));
  }else {
    return ((x));
}
