#include "holberton.h"
/**
 *
 **/
unsigned int _strspn(char *s, char *accept)
{
	int i, j;
	for (j = 1; s[j]; j++)
	{
		for ( i = 1; accept[i]; i++)
		{
			if (s[j] == accept[i])
return(i);
		}
	}
	return (i);       
}
