#include "holberton.h"
/**
 *is_prime_number - return 1 if its prime
 *@n: number
 *Return: 1 or 0
 */
int is_prime_number(int n)
{
	int i;
	if (i == 1)
	{
		return (1);
	}
	else if (n %i == 0)
	{
		return (0);
	}
	else

		i = i - 1;
		is_prime_number(n);

}
